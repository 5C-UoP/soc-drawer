/// A class that represents a society model
class Socieity {
  const Socieity(this.id, this.name, this.icon, this.joined);

  final int id;
  final String name;
  final String icon;
  final bool joined;
}
